# Production build — points the whole frontend at the deployed Render backend.
# Committed (not gitignored) so Vercel / any production `vite build` bakes this in.
# To repoint, change this one value and rebuild/redeploy.
VITE_API_BASE_URL=https://bis-assistant-1.onrender.com
